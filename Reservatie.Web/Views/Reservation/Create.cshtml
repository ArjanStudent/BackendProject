
@model Reservatie.Web.ViewModels.ReservationVM

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Reservation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Reservation.Screening" class="control-label"></label>
                <input asp-for="Reservation.Screening" class="form-control" />
                <span asp-validation-for="Reservation.Screening" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label">Movie name</label>
                <select asp-for="Reservation.Screening.Id" class="form-control" asp-items="@Model.screening"></select>
                <span asp-validation-for="Reservation.Screening.Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
